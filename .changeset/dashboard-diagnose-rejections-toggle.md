---
"@mcpeak/dashboard": patch
---

대시보드가 사용자가 답한 값을 터미널 출력에 되비추고, generate 폼 4단계에 `--diagnose-rejections` 토글을 더한다.

- 웹 검토 입출력이 답을 받는 순간 질문과 답을 한 줄 찍는다. 터미널에서 readline 이 하던 일이다. 앞선 질문에 무엇이라고 답했는지가 화면에 남는다.
- 4단계에 `거절 근거 진단 보기` 토글이 생긴다. 시험 실행이 꺼져 있으면 비활성이고, 켜 둔 채 시험 실행을 끄면 함께 내려간다.
