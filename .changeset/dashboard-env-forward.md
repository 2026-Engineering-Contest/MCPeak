---
"@mcpeak/dashboard": minor
---

`.mcp.json` 의 `env` 를 자식 서버에 넘깁니다. 이름은 `--env <NAME>` 으로 argv 에 실리고 값은 대시보드 서버 프로세스 안에서만 해석되므로, 브라우저에도 실행 명령에도 비밀값이 남지 않습니다.
서버 목록의 안내문이 바뀌었습니다. "env 는 대시보드가 넘기지 못합니다" 대신 어떤 이름을 넘기는지 보여줍니다.
녹화 출처의 인자가 가려진 녹화본은 원클릭 재생 대신 입력 폼이 열리고, 무엇이 가려졌고 어떻게 하면 되는지 말합니다.
