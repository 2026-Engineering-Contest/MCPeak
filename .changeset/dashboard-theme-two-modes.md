---
"@mcpeak/dashboard": minor
---

dashboard: 테마 버튼을 라이트·다크 2값 토글로 바꿉니다. `시스템`은 선택지에서 빠지고 "아직
고르지 않음" 이라는 내부 상태가 됩니다 — 한 번도 안 고른 사용자는 지금처럼 OS 설정을 실시간으로
따라가고, 버튼을 누르는 순간 그 값으로 고정됩니다. 라벨이 `테마: 시스템` 대신 지금 켜져 있는
모드를 그대로 보여줍니다. 기존 저장값은 그대로 쓰이며 마이그레이션이 필요 없습니다.
