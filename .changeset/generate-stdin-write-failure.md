---
"@ohmymcp/generate": patch
---

stdin 쓰기 오류를 예외 없이 실패로 판정합니다. 이전에는 종료 코드가 0이고 stdout이 유효한 JSON이면 무시했지만, 쓰기 오류가 났다는 것은 프롬프트 일부가 전달되지 않았다는 뜻이고 그 응답은 잘린 입력에 대한 응답입니다. 오류가 나면 자식 프로세스를 정리하고 `internal`로 보고합니다.
