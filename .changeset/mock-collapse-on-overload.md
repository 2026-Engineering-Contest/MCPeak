---
---

발행되는 패키지가 바뀌지 않아 빈 changeset 입니다.

`MockServer.on()` 의 오버로드 두 개를 `options?` 하나로 줄였습니다. 구현부는 이미
`options?: { isError?: boolean }` 였고 3인자·4인자 호출이 모두 그대로 통과하므로,
타입 선언만 줄어들고 소비자 쪽 호환은 바뀌지 않습니다. 나머지는 주석입니다.
