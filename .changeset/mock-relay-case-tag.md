---
"@mcpeak/mock": minor
---

중계기가 `req.url` 의 `case` 쿼리 파라미터를 읽어 `--json` 기록 줄에 싣는다. 요청·응답
양쪽에 실리고, 200 자를 넘거나 비어 있으면 싣지 않는다(`MAX_CASE_TAG`) — 사람이 읽는 줄은
바뀌지 않는다. 같은 중계기에 여러 접속이 동시에 붙을 때(대시보드 4 단계 「실제 응답」이
케이스마다 붙는 것처럼) 기록 줄을 접속 단위로 가를 수 있게 한다(ADR-0105).
