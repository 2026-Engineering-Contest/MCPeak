---
"@mcpeak/record": patch
---

녹화 세션에 저장되는 실행 출처(`origin.args`)에서 비밀 모양의 인자를 가립니다. 지금까지는
서버를 띄운 명령줄이 원문 그대로 세션 파일에 실려, 인자로 토큰을 받는 서버를 녹화하면 그 값이
저장소에 커밋되는 카세트로 따라 들어갔습니다. 이제 엔진이 세션을 만들기 전에 민감 키 이름
뒤의 값과 알려진 비밀 접두·JWT 모양의 값을 `[redacted]` 로 바꿔 넘기며, Store 는 받은 값을
그대로 저장한다는 계약을 유지합니다.
