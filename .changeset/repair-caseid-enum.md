---
"@ohmymcp/generate": patch
---

진단 요청의 `caseId` 허용 값을 요청마다 스키마 `enum` 으로 못 박습니다. provider 가 여러 케이스를 한 항목에 이어 붙여 답하면 검증이 그 항목을 버려, 근거가 충분한 답이 통째로 `unsure` 로 접히던 문제를 없앱니다. 프롬프트도 허용 목록과 "여러 케이스가 같은 원인이면 항목을 나눠 각각 낸다" 는 규칙을 함께 싣습니다.
