---
"@mcpeak/runner": minor
"@mcpeak/cli": patch
---

입력 스키마의 nullable `anyOf`/`oneOf` 필드를 값 갈래로 해석해 타입·enum·범위 축을 만든다 (#426)
`additionalProperties: false` 툴에 선언 밖 필드 거절을 검증하는 `UNDECLARED_FIELD` 축을 더한다 (#427)
