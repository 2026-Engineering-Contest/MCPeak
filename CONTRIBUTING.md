# 개발 규칙 (Team Charter)

> 이 문서는 5인 팀이 마감(8월 27일)까지 **서로 충돌하지 않고, 각자 방어 가능한 기여 이력을 남기며** 개발하기 위한 규칙이다.
> 규칙을 바꾸려면 PR로 이 문서를 수정하고 오너 과반의 승인을 받는다. 구두 합의는 규칙이 아니다.

| 항목 | 값 |
|---|---|
| 문서 버전 | v1.1 |
| 적용 시작 | 2026-07-28 |
| 코드 동결 | 2026-08-24 |
| 제출 마감 | 2026-08-27 |
| 라이선스 | **MIT (확정)** |
| 패키지 스코프 | `@mcp-test/*` (npm 명칭 확정 후 일괄 치환) |

---

## 1. 원칙

1. **소유권은 패키지 단위로 나눈다.** 기능 단위로 나누면 같은 파일을 여러 명이 고치게 되고, `git blame`이 섞여 개인 기여를 증명할 수 없게 된다.
2. **인터페이스를 먼저 얼리고, 구현은 병렬로 한다.** 코어 완성을 기다리는 사람이 한 명도 없어야 한다.
3. **모든 결정은 흔적을 남긴다.** 릴리스 노트, ADR, PR 리뷰 세 가지가 개인 기여의 증거다.
4. **막판에 무엇을 버릴지 미리 정한다.** 일정이 밀렸을 때 협상하지 않기 위해서다.

---

## 2. 저장소 구조와 소유권

```
mcp-test/
├── .changeset/                 # 릴리스 노트 조각
├── .github/
│   ├── CODEOWNERS              # 패키지별 리뷰어 자동 지정
│   ├── ISSUE_TEMPLATE/
│   ├── pull_request_template.md
│   └── workflows/
│       ├── ci.yml              # PR마다: 타입체크 · 린트 · 테스트 · 빌드
│       └── release.yml         # main 머지 시: 버전 PR 생성 / npm 배포
├── docs/
│   └── adr/                    # 설계 결정 기록 (0001-*.md)
├── packages/
│   ├── core/                   # 프로토콜 클라이언트 · 트랜스포트 · 프로세스 수명주기
│   ├── runner/                 # createMcpTest · matcher · 리포터
│   ├── generate/               # 스키마 → 테스트 코드 생성
│   ├── record/                 # 녹화 · 재생 · 계약 스냅샷
│   ├── mock/                   # 목 서버 · 가짜 데이터 생성
│   └── cli/                    # npx mcp-test 진입점 (얇게 유지)
├── examples/                   # 도그푸딩 대상 · 데모용 예제 서버
├── fixtures/                   # 공용 테스트 픽스처 (JSON 응답 샘플)
├── CONTRIBUTING.md             # 이 문서
├── README.md
├── pnpm-workspace.yaml
└── turbo.json
```

### 2.1 오너 표

각 패키지에는 **오너 1명**이 있다. 오너는 그 패키지의 설계 결정권과 최종 책임을 가진다.

| 패키지 | 오너 | 책임 범위 |
|---|---|---|
| `core` | (이름) | 트랜스포트, 프로세스 기동·종료, 타임아웃, stderr 수집, 핸드셰이크 |
| `runner` | (이름) | 공개 API 설계, matcher, **실패 메시지 품질**, 리포터, JUnit XML |
| `generate` | (이름) | 툴 스키마 분석, 테스트 케이스 합성, 코드 생성 |
| `record` | (이름) | 카세트 포맷, 요청 매칭 키, 비결정 필드 처리, 계약 스냅샷 |
| `mock` + 릴리스 | (이름) | 가짜 데이터 생성, 응답 주입 API, npm 배포·버저닝, **도그푸딩** |
| `cli` | 공동 | 각 오너가 자기 서브커맨드만 수정 |

### 2.2 소유권 규칙

- **다른 사람의 패키지를 직접 고치지 않는다.** 필요하면 이슈를 열고 오너에게 넘긴다.
- 예외: 빌드가 깨져 전원이 막힌 경우. 이때는 최소 수정 후 오너를 리뷰어로 지정하고 이슈에 사유를 남긴다.
- `cli`와 `fixtures`는 공용이지만, 한 PR에서 여러 오너의 영역을 동시에 건드리지 않는다.

---

## 3. 인터페이스 동결 규칙

**W1(첫 주) 안에 패키지 간 경계 타입을 확정하고, 그 시점부터 "동결"로 취급한다.**

```ts
// packages/core/src/types.ts — 이 파일이 모든 병렬 작업의 기준점이다
export interface McpClient {
  listTools(): Promise<ToolDef[]>;
  callTool(name: string, args: unknown): Promise<ToolResult>;
  close(): Promise<void>;
}

export type ToolResult = {
  content: unknown;
  isError: boolean;
  raw: unknown; // 원본 응답. 녹화·스냅샷은 이 필드를 기준으로 한다
};
```

- 동결 이후 이 타입을 바꾸려면 **PR + 영향받는 오너 전원의 승인**이 필요하다.
- 동결 전까지 `core` 외 패키지는 `fixtures/`의 JSON으로 개발한다. 실제 서버를 기다리지 않는다.
- 타입 변경 PR은 제목에 `[BREAKING]`을 붙인다.

> 이 절차 자체가 제안서 기능 4-3(계약 우선 워크플로우)의 실증 사례가 된다. 발표에서 근거로 쓸 수 있도록 W1 합의 과정을 ADR로 남긴다.

---

## 4. 브랜치 · 커밋 규칙

### 브랜치

```
main                    # 항상 배포 가능한 상태. 직접 푸시 금지
feat/<패키지>-<요약>    # 예: feat/record-cassette-format
fix/<패키지>-<요약>
docs/<요약>
```

### 커밋 (Conventional Commits)

```
<type>(<scope>): <요약>

feat(runner): toMatchSchema matcher 추가
fix(core): 서버 종료 시 좀비 프로세스 잔존 문제 해결
docs(adr): 카세트 매칭 키 결정 기록
```

- `type`: `feat` `fix` `docs` `test` `refactor` `chore` `ci`
- `scope`: 패키지명(`core` `runner` `generate` `record` `mock` `cli`)
- **scope는 필수다.** 나중에 `git log --grep "(record)"` 한 줄로 개인 기여를 뽑아낼 수 있어야 한다.

---

## 5. PR 규칙

1. **main 직접 푸시 금지.** 모든 변경은 PR을 거친다.
2. **리뷰어는 자기 패키지가 아닌 오너 1명 이상.** 리뷰 코멘트도 공개 기여 이력이므로, 승인만 누르지 말고 최소 1개의 질문이나 지적을 남긴다.
3. **CI 통과 없이 머지 금지.** 타입체크 · 린트 · 테스트 · 빌드 4종.
4. **PR은 작게.** 변경 400줄을 넘으면 쪼갠다. 리뷰가 형식적으로 변하는 지점이다.
5. **기능 PR에는 changeset을 포함한다** (7절 참조). 없으면 CI가 경고한다.
6. 24시간 내 리뷰가 없으면 팀 채널에서 호출한다. 대기가 병목의 1순위다.

### PR 템플릿 (`.github/pull_request_template.md`)

```markdown
## 무엇을 바꿨나

## 왜 이렇게 했나
<!-- 설계 판단이 있었다면 ADR 링크. 이 칸이 나중에 면접 답변이 된다 -->

## 어떻게 검증했나
<!-- 추가/수정한 테스트 -->

## 체크리스트
- [ ] 내 패키지 범위 안에서만 수정했다
- [ ] 테스트를 추가했다
- [ ] 공개 API가 바뀌었다면 changeset을 추가했다
- [ ] 공개 API가 바뀌었다면 README를 갱신했다
```

---

## 6. 품질 게이트

CI에서 자동 검사한다. 로컬에서는 커밋 훅으로 린트·포맷만 돌린다.

| 검사 | 목적 | 실패 시 |
|---|---|---|
| 타입체크 | 패키지 간 계약 위반 탐지 | 머지 차단 |
| 린트 · 포맷 | 스타일 논쟁 제거 | 머지 차단 |
| 단위 테스트 | 기능 회귀 탐지 | 머지 차단 |
| 빌드 | 배포 산출물 검증 | 머지 차단 |
| 패키지 메타 검증 | `exports` · 타입 정의 오류 탐지 | 경고 (배포 전 필수) |
| Node 20 / 22 / 24 매트릭스 | 사용자 환경 호환성 | 머지 차단 |

- **포맷 논쟁 금지.** 포매터 설정이 정답이다. 리뷰에서 스타일 지적은 하지 않는다.
- **우리 도구로 우리를 검증한다.** `examples/`의 예제 서버에 `mcp-test`를 적용하는 E2E를 CI에 포함한다. 이 항목이 깨지면 곧 사용자에게 깨진 것이다.

---

## 7. 릴리스 규칙

### 버전

- `0.x.y`로 시작한다. 마감 전까지 **breaking change를 허용**하되 CHANGELOG에 반드시 남긴다.
- 2주차 알파(`0.1.0-alpha.x`) 배포를 반드시 지킨다. 배포되지 않은 코드는 심사에서 증명하기 어렵다.

### 절차

1. 기능 PR에 changeset 파일을 포함한다 (`pnpm changeset` → 변경 종류와 한 줄 설명 입력).
2. main에 머지되면 릴리스 워크플로가 **버전 PR**을 자동 생성한다.
3. 릴리스 담당이 버전 PR을 머지하면 npm에 배포되고 CHANGELOG가 갱신된다.

> **changeset 설명은 곧 릴리스 노트다.** "fix bug" 같이 쓰면 본인 기여 기록이 그렇게 남는다. `record: 카세트 매칭 시 타임스탬프 필드를 비교 대상에서 제외` 수준으로 쓴다.

### 배포 권한

- npm 배포는 릴리스 담당 1명이 관리하되, **워크플로에서만 배포**한다. 로컬 `npm publish` 금지.
- 장기 토큰 대신 GitHub Actions의 신뢰 발행(OIDC) 방식을 우선 검토한다.

---

## 8. ADR (설계 결정 기록)

**오너 1인당 최소 2건**을 마감까지 작성한다. 분량은 한 페이지면 충분하다.

- 위치: `docs/adr/0001-제목.md`
- 대상: "다르게 갈 수도 있었던 결정". 단순 구현은 대상이 아니다.

### 템플릿

```markdown
# ADR-0001: <결정 제목>

- 상태: 제안 / 채택 / 폐기
- 날짜: 2026-08-05
- 작성자: <이름>

## 배경
어떤 문제를 만났나.

## 선택지
- A안: ...
- B안: ...

## 결정
무엇을 골랐나.

## 이유
왜 그것인가. 무엇을 포기했나.

## 결과
이 결정으로 생긴 제약과 후속 과제.
```

### 초기 ADR 후보

| 번호 | 주제 | 담당 |
|---|---|---|
| 0001 | 트랜스포트 전략: 프로세스 기동(stdio) vs 인프로세스 연결 — 어느 것을 기본으로 둘 것인가 | core |
| 0002 | matcher를 기존 테스트 러너 확장으로 만들 것인가, 독립 구현으로 갈 것인가 | runner |
| 0003 | 카세트 매칭 키 설계와 비결정 필드(타임스탬프 등) 처리 | record |
| 0004 | 생성 테스트의 범위: 어디까지 자동 생성하고 어디부터 사람이 쓰게 할 것인가 | generate |
| 0005 | 목 데이터 생성 전략: 스키마 기반 랜덤 vs 고정 시드 | mock |

---

## 9. 통합 리듬

| 주차 | 목표 | 통합일 검증 항목 |
|---|---|---|
| W1 (7/28–8/3) | 인터페이스 동결, 5개 패키지 스켈레톤, CI 구축 | 각 패키지가 픽스처 기준 테스트 통과 |
| W2 (8/4–8/10) | `core` + `runner` 결합, 첫 E2E, **npm 알파 배포** | 실제 서버 1개에 `npx mcp-test` 성공 |
| W3 (8/11–8/17) | 녹화·재생 통합, 목 서버 동작, **도그푸딩 PR 1차** | `--record` → `--replay` 왕복 성공 |
| W4 (8/18–8/24) | 계약 스냅샷 데모, 문서·데모 영상, 발표자료 | **8/24 코드 동결** |
| 8/25–8/27 | 제출 버퍼 (코드 변경 금지) | — |

- **통합은 주 2회(화·금) 고정.** 5인 모노레포에서 지연 원인 1순위는 기술이 아니라 통합 미루기다.
- 통합일에는 각자 브랜치를 main에 올려 붙여 보고, 깨진 지점을 그 자리에서 이슈로 만든다.

---

## 10. 이슈 · 도그푸딩 규칙

- 버그·요청은 전부 이슈로 만든다. 채팅에서 논의하고 끝내지 않는다.
- 라벨: `pkg:core` `pkg:runner` … `type:bug` `type:feat` `good-first-issue`
- **도그푸딩 규칙**: 공개된 외부 MCP 서버 3~5개를 선정해 우리 도구로 테스트를 붙이고, 가능하면 해당 저장소에 PR을 보낸다.
  - 적용 중 발견한 문제는 반드시 이슈로 등록하고 담당 오너에게 배정한다.
  - 외부 저장소에 PR을 보낼 때는 상대 프로젝트의 기여 규칙을 먼저 읽는다. 홍보성 PR로 보이면 역효과다.
  - 도입 실적(적용 저장소 수, 발견·수정한 결함 수)을 `docs/adoption.md`에 누적 기록한다. 심사 자료이자 개인 성과 근거다.

---

## 11. 범위 축소 순서 (사전 합의)

일정이 밀리면 **아래 순서대로 잘라낸다.** 그 시점에 다시 논의하지 않는다.

1. 계약 스냅샷 → 데모 수준으로 축소
2. `init` 스캐폴딩 → 템플릿 복사 수준으로 축소
3. 목 서버 → 응답 주입 기능만 유지
4. 녹화·재생 → 녹화만 유지

**기능 1(테스트 러너)과 기능 4-2(테스트 자동 생성)는 차별점이므로 끝까지 지킨다.** 이 둘이 빠지면 프로젝트의 정체성이 사라진다.

---

## 12. 개인 기여 증거 체크리스트

마감 시점에 **전원이** 아래를 갖추고 있어야 한다. 주 1회 스스로 점검한다.

- [ ] 내가 오너인 패키지가 있고, 그 패키지의 커밋 대부분이 내 것이다
- [ ] 내 이름으로 된 CHANGELOG 항목이 3건 이상 있다
- [ ] 내가 쓴 ADR이 2건 이상 있다
- [ ] 다른 사람 PR에 남긴 리뷰 코멘트가 10건 이상 있다
- [ ] 내가 해결한 "어려웠던 문제"를 3분 안에 설명할 수 있다
- [ ] 그 문제의 재현 조건과 해결 근거가 이슈나 ADR에 남아 있다

> 마지막 두 항목이 핵심이다. 면접에서 묻는 것은 "무엇을 만들었나"가 아니라 **"왜 그렇게 했나"**다.

---

## 13. 라이선스 · 의존성 정책

### 13.1 라이선스: MIT

파생물 공개 의무 없이 출처 표시만 요구하는 permissive 라이선스를 택한다. 개발자 도구는 도입 마찰이 곧 사용자 수이므로, 사용 조건이 까다로우면 채택되지 않는다.

### 13.2 라이선스 표기는 세 곳 모두

한 곳이라도 빠지면 npm 페이지에 `UNLICENSED`로 표시되어, 기업 환경에서는 도입 검토 단계에서 걸러진다.

| 위치 | 내용 |
|---|---|
| 저장소 루트 `LICENSE` | MIT 전문 + 저작권자 표기 |
| 각 패키지 `package.json` | `"license": "MIT"` — 6개 패키지 전부 |
| README 상단 | 라이선스 배지 |

- 저작권자 표기는 개인명이 아닌 **팀명 또는 프로젝트명**으로 통일한다. 개인명을 쓰면 나중에 이관이 번거롭다.
- 릴리스 담당이 배포 전에 6개 `package.json`을 일괄 확인한다.

### 13.3 의존성 라이선스 관리

`docs/licenses.md`에 사용 중인 의존성과 라이선스를 표로 유지한다. 릴리스 담당이 소유하며, 의존성 추가 시 갱신한다.

| 패키지 | 버전 | 라이선스 | 용도 |
|---|---|---|---|
| (예) `@modelcontextprotocol/sdk` | 1.x.y (고정) | MIT | MCP 프로토콜 클라이언트 |

- **금지**: GPL·AGPL 계열을 런타임 의존성으로 넣지 않는다. 우리 라이선스와 충돌한다. 개발 도구(devDependencies)는 예외.
- 의존성 추가는 PR에서 **용도와 라이선스를 명시**해야 승인한다.
- 심사·발표에서 "무엇을 가져다 썼는가" 질문이 나오면 이 표로 답한다.

### 13.4 외부 의존성 버전 정책

- `@modelcontextprotocol/sdk`는 **1.x 안정 버전으로 고정**한다(`^` 금지). 분리 패키지 구조의 2.0이 개발 중이므로, 자동 업그레이드되면 `core`가 통째로 깨진다.
- SDK 버전 변경은 `core` 오너만 제안할 수 있고, `[BREAKING]` PR로 처리한다.
- **8월 24일 코드 동결 이후 어떤 의존성도 올리지 않는다.**

### 13.5 공개 규칙

- 저장소는 **W1부터 public**으로 둔다. 마감 직전 공개는 커밋 이력이 며칠치로만 보여 오히려 불리하다.
- README에는 최소한 다음을 담는다: 한 줄 소개 / 설치 / 30초 안에 따라 할 수 있는 예제 / 기존 도구와의 차이 / 라이선스.
- 비밀값(API 키, 토큰)은 커밋하지 않는다. 녹화 파일(`__recordings__/`)에 인증 헤더가 섞이지 않도록 `record` 오너가 마스킹 규칙을 정하고 문서화한다.
- 외부 기여자를 받을 수 있도록 `good-first-issue` 라벨을 3건 이상 유지한다.

---

## 부록 A. CODEOWNERS

```
# .github/CODEOWNERS — 해당 경로 변경 시 오너가 자동으로 리뷰어로 지정된다
/packages/core/       @core-owner
/packages/runner/     @runner-owner
/packages/generate/   @generate-owner
/packages/record/     @record-owner
/packages/mock/       @mock-owner
/packages/cli/        @runner-owner @mock-owner
/docs/adr/            @core-owner
/CONTRIBUTING.md      @core-owner @runner-owner
```

## 부록 B. 권장 툴체인

| 영역 | 선택 | 이유 |
|---|---|---|
| 패키지 매니저 | pnpm (workspace) | 모노레포 표준. `workspace:` 프로토콜로 패키지 간 참조가 명시적 |
| 의존성 버전 통일 | pnpm catalog | 5개 패키지의 버전 드리프트 방지 |
| 태스크 실행 | Turborepo | 변경된 패키지만 빌드·테스트. 없어도 되지만 CI 시간이 줄어든다 |
| 테스트 | Vitest | TS 네이티브. 우리가 만드는 것도 테스트 도구이므로 내부 러너와 개념이 겹치지 않게 경계를 문서화할 것 |
| 린트 · 포맷 | Biome (또는 oxlint + oxfmt) | 설정 1개로 린트·포맷 통합. 스타일 논쟁 시간 절약 |
| 번들 | tsdown 또는 tsup | ESM/CJS 동시 출력과 타입 선언 생성 |
| 릴리스 | Changesets | PR 단위 릴리스 노트가 그대로 개인 기여 기록이 된다 |
| 패키지 검증 | publint, are-the-types-wrong | `exports`·타입 경로 오류는 배포 후에야 드러난다 |
| 미사용 코드 탐지 | knip | 마감 직전 정리에 유용 |
| 커밋 훅 | lefthook 또는 husky | 커밋 시 린트·포맷 자동 적용 |
| PR 미리보기 배포 | pkg.pr.new | PR마다 설치 가능한 프리뷰 패키지 생성. 도그푸딩 대상에게 "이 버전 써보세요"가 가능해진다 |

## 부록 C. 첫 주 셋업 순서

1. 저장소 생성 · main 브랜치 보호 규칙(리뷰 1인, CI 필수) 설정
2. pnpm workspace + 5개 패키지 스켈레톤 생성
3. `core/src/types.ts` 인터페이스 합의 및 동결 (3절)
4. `fixtures/`에 실제 MCP 서버 응답 샘플 확보 — 이후 4명의 병렬 개발 재료
5. CI 워크플로 구축 (타입체크 · 린트 · 테스트 · 빌드)
6. CODEOWNERS · PR 템플릿 · 이슈 템플릿 배치
7. Changesets 초기화, 알파 배포 리허설 1회
8. ADR-0001 ~ 0005 뼈대 파일 생성 (내용은 결정 시점에 채움)
