# ADR-0039: 민감 키 목록에 `cookie` 를 넣고 매칭을 접미 단어열 일치로 좁힌다 (ADR-0003 개정)

- 상태: 채택
- 날짜: 2026-08-18
- 담당: record
- 작성자: @ddxng5 (② replay/record 파트)
- 승인: 승인 (2026-08-18, @sunghoon0303 · 파트 ① 오너 — 파트 밖 승인)
- 개정 (2026-08-19): [ADR-0045](./0045-민감-키-목록의-복수형과-key-합성어.md) 목록에 `key`
  합성어를 넣고 복수형을 조회에서 흡수한다. `채택` 됐다. 아래 「결정」의 목록과 판정 문단은
  그 개정 전 내용이므로 현재 동작은 0045 를 함께 본다.
- 참조: `docs/adr/0003-cassette-matching-key.md` (개정 대상),
  `docs/adr/0040-스키마와-데이터의-마스킹-규칙-분리.md`,
  `docs/adr/0041-마스킹의-적용-경계.md`,
  [PR #169](https://github.com/2026-Engineering-Contest/OhMyMCP/pull/169)

## 배경

ADR-0003 이 정한 기본 마스킹 목록은 일곱 개다 — `authorization`, `apikey`, `accesstoken`,
`refreshtoken`, `token`, `secret`, `password`. 이 목록이 두 방향으로 어긋나 있다.

**빠진 것.** `cookie` 와 `set-cookie` 가 없다. `authorization` 은 목록에 있는데, 세션
식별자를 나른다는 점에서 같은 급인 `Cookie` 가 없는 것은 일관성 결함이다. HTTP 응답 헤더를
그대로 돌려주는 툴이라면 세션 값이 카세트 파일과 경고 출력에 원문으로 남는다.
`normalizeKey` 가 `-` 와 `_` 를 제거하므로 `Set-Cookie` → `setcookie` 가 되어, 항목 하나로
둘 다 덮인다.

**과하게 잡는 것.** `sensitiveKey` 가 `includes` 부분일치라 `tokenCount` · `secretariat` ·
`passwordPolicy` 가 전부 마스킹된다. ADR-0003 이 "비밀값 마스킹은 정확성보다 안전을
우선한다"며 **명시적으로 감수한** 트레이드오프다.

두 문제가 서로 얽혀 있다. 부분일치를 유지한 채 `cookie` 만 추가하면 `cookieCount` ·
`cookiePolicy` · `cookieConsent` 가 새로 걸려 오탐이 곱해진다.

## 선택지

1. **`cookie` 만 추가한다.** 매칭 방식은 그대로 둔다.
2. **매칭만 좁힌다.** `cookie` 는 넣지 않는다.
3. **둘 다 한다.** 항목을 추가하고 매칭을 단어 경계 기준으로 좁힌다.
4. **사용자 정의 목록을 연다.** 기본값 논쟁을 사용자에게 넘긴다.

## 결정

**선택지 3 을 고른다.**

**목록에 `cookie` 를 추가한다.** `setcookie` 는 넣지 않는다 — 아래 접미 규칙에서
`Set-Cookie` 가 `cookie` 로 이미 걸리므로 중복 항목이 된다.

**매칭을 `includes` 부분일치에서 접미 단어열 일치로 좁힌다.**

1. 키를 구분자(`-` · `_`)와 카멜케이스 경계로 쪼갠다. `X-Api-Key` → `[x, api, key]`
2. 각 단어의 꼬리 숫자를 뗀다. `apiKey0` → `[api, key]`
3. **뒤에서부터** 이어붙인 조합이 목록과 **정확히** 일치하면 민감으로 본다

| 키 | 단어 | 접미 조합 | 판정 |
|---|---|---|---|
| `Set-Cookie` | set · cookie | `cookie` | 마스킹 |
| `accessToken` · `sessionToken` | … · token | `token` | 마스킹 |
| `X-Api-Key` · `apiKey0` | x · api · key | `key` → `apikey` | 마스킹 |
| `tokenCount` | token · count | `count` · `tokencount` | 통과 |
| `passwordPolicy` | password · policy | `policy` · `passwordpolicy` | 통과 |
| `secretariat` | secretariat | `secretariat` | 통과 |

한 단어씩만 보지 않는 이유는 `X-Api-Key` 다. 마지막 단어 `key` 는 목록에 없고 `apikey` 가
있다. 꼬리 숫자를 떼는 이유는 `apiKey0` 이 여전히 API 키이기 때문이며, 머리 명사를 바꾸지
않으므로 `cookieCount2` 가 새로 걸리지도 않는다.

**`normalizeKey` 는 건드리지 않는다.** 그 함수는 구분자를 지우므로 경계 정보가 사라지고,
`NONDETERMINISTIC_KEYS` 조회와 공유하므로 여기를 바꾸면 비결정 필드 처리까지 흔들린다.
단어 분리는 별도 함수로 둔다.

**사용자 정의 마스킹 규칙은 여전히 제공하지 않는다.** ADR-0003 의 이 부분은 유지한다.

세 개정 중 **가장 먼저** 구현한다.

## 이유

**`Cookie` 는 `Authorization` 과 같은 급이다.** 둘 다 요청자의 신원을 증명하는 값을 나른다.
하나만 가리는 목록은 근거가 없다.

**항목을 늘리면서 매칭을 그대로 두면 손해가 커진다.** 부분일치는 항목 수에 비례해 오탐을
만든다. 지금도 `tokenCount` 가 걸리는데 여기에 `cookie` 를 얹으면 웹 콘텐츠를 다루는 툴에서
오탐이 눈에 띄게 늘어난다. 이 레포의 E2E 가 쓰는 `mcp-server-fetch` 가 정확히 그런 서버다.

**과잉 마스킹의 비용이 곧 바뀐다.** ADR-0003 이 과잉을 감수한 것은 비용이 "카세트 파일이 좀
지저분하다"였기 때문이다. 그 판단은 그때 타당했다. 그런데 ADR-0041 이 마스킹을 **테스트가
보는 값**에까지 닿게 하면 비용이 "테스트가 그 필드를 영영 못 본다"로 바뀐다. 같은
트레이드오프가 아니므로 판단도 같을 이유가 없다. 이 ADR 이 0041 보다 먼저 가는 이유가
그것이다 — 먼저 좁혀 두면 0041 의 폭발 반경이 작아진다.

**선택지 4 를 버린 이유.** 기본값이 틀린 채로 옵션을 열면 대부분의 사용자는 틀린 기본값을
쓴다. ADR-0003 이 첫 버전에서 사용자 정의 규칙을 배제한 판단을 유지한다.

## 결과

- ADR-0003 의 기본 마스킹 목록 문단과 "정확성보다 안전 우선" 문단을 개정한다.
- **카세트 파일 내용이 바뀐다.** `cookie` 계열이 새로 가려지고 `tokenCount` 계열은 안
  가려진다. 포맷과 `CASSETTE_VERSION` 은 바뀌지 않으므로 기존 카세트는 그대로 로드되지만,
  재녹화 전까지 옛 마스킹 결과를 갖는다.
- `packages/record/tests/index.test.ts` 의 `tokenCount: "[redacted]"` 기대값이 바뀐다. 이
  단언은 ADR-0003 이 승인한 동작을 고정한 것이므로, 수정은 정리가 아니라 결정을 바꾸는 행위다.
- 규칙의 한계를 남긴다. `authorizationHeader` 는 접미가 `header` 라 걸리지 않는다. 합성명사의
  머리를 보는 규칙이므로 영어가 아닌 키에는 적용되지 않는다.

### 카세트 호환성 계약 (ADR-0039 · 0040 · 0041 공통)

세 개정 모두 저장되는 **값**을 바꾸지만 카세트의 **형식**은 바꾸지 않는다. 그래서 다음을 계약으로
못 박는다. 0040 과 0041 은 이 절을 참조한다.

- **`CASSETTE_VERSION` 을 올리지 않는다.** 바뀌는 것은 포맷이 아니라 마스킹 **정책**이다.
  형식이 같으므로 구형 카세트도 계속 로드된다.
- **카세트에 담긴 값은 녹화 당시 정책의 결과다.** 정책이 바뀐 뒤 구형 카세트를 재생하면 새
  정책으로 녹화한 것과 다른 값이 나온다. 예를 들어 구형 카세트의 `tokenCount` 는 문자열
  `"[redacted]"` 로 굳어 있고, 0039 이후 새로 녹화하면 숫자 `42` 다.
- **되돌릴 수 없다.** 마스킹은 비가역이므로 구형 카세트에서 원래 값을 복구할 방법은 없다.
  **정책이 바뀌면 재녹화가 필요하다.**
- **구형 카세트를 자동으로 거부하지 않는다.** 거부하려면 카세트가 자신이 어느 정책으로
  녹화됐는지 알아야 하고, 그 표식을 넣는 것이 곧 포맷 변경이다. ADR-0041 의 선택지 4(마스킹을
  메타데이터로 기록, 카세트 v2)와 함께 다룬다. **그때까지 이 불일치는 릴리스 노트로만 알린다.**
