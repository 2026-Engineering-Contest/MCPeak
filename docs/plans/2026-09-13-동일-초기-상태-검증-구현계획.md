# 동일 초기 상태 검증 구현계획

설계 문서: [후보 비교와 최종 검증은 같은 초기 상태에서 한다](../2026-09-13-후보-비교와-최종-검증은-같은-초기-상태에서-한다-설계.md)
대상 이슈: #399 (#398 통합)
선행: #387 · #401 · #388 의 `cli` 태스크가 전부 머지돼 있어야 한다. `generate-command.ts` 를
같이 고치므로 동시에 진행하면 충돌한다.

## 1. 실행 모델

메인 세션은 오케스트레이터다. 스폰·리뷰·머지 게이트만 하고 코드를 쓰지 않는다.

## 2. 터미널 분할과 웨이브

두 태스크가 순차다. T2 는 T1 이 만든 초기화 등급 타입을 쓴다.

| 웨이브 | 태스크 | 패키지 | worktree / 브랜치 | 모델 |
|---|---|---|---|---|
| 1 | T1 초기화를 등급으로 만들고 후보 비교를 같은 상태에서 한다 | `cli` | `.claude/worktrees/mcpeak-399-t1` / `feat/reset-grade-and-prefill-compare` | 상위 |
| 2 | T2 최종 명세를 처음부터 재검증한다 | `cli` | `.claude/worktrees/mcpeak-399-t2` / `feat/final-suite-revalidation` | 상위 |

**둘 다 상위 모델이다.** 상태 전이 조건과 "언제 채택하지 않는가" 의 판단이 들어가고, 실제
서버 호출을 늘리는 경계(#397)를 지켜야 한다. `CLAUDE.local.md` 모델 표의 "계획서에 코드로
못 박기 어려운 판단" 에 해당한다.

두 태스크가 같은 패키지·같은 파일(`generate-command.ts`)을 고치므로 **반드시 순차**다.

## 3. 수정 금지 공유 계약

- `packages/core/src/types.ts` 의 `McpClient` · `ToolResult`
- `packages/runner` · `packages/generate` 전체. 이번 작업은 `cli` 안에서 끝난다
- 루트 `package.json` · `pnpm-workspace.yaml` · `turbo.json` · `tsconfig.base.json`
- `@modelcontextprotocol/sdk` 버전(1.x 고정)
- 목록 밖 의존성 추가 금지
- 커밋 · 머지 · 푸시 금지

**패키지 이름 주의.** CLI 의 실제 이름은 `@mcpeak/cli` 다. `pnpm --filter mcpeak` 는
`No projects matched the filters` 를 찍고 **종료 코드 0 으로 끝난다.**

---

## T1. 초기화를 등급으로 만들고 후보 비교를 같은 상태에서 한다 (`cli`)

### Files

수정:
- `packages/cli/src/reset-hook.ts`
- `packages/cli/src/pre-fill-wiring.ts`
- `packages/cli/src/generate-command.ts`
- `packages/cli/tests/reset-hook-e2e.test.ts`
- `packages/cli/tests/pre-fill-wiring.test.ts`
- `packages/cli/tests/generate-command.test.ts`
- `packages/cli/tests/pre-fill-command.test.ts`

생성:
- `.changeset/<임의이름>.md`

**`pre-fill-command.test.ts` 의 기존 테스트를 지우거나 기대를 뒤집지 마라.** 2026-09-13 실측에서
`"baseline 이 실패하고 제안이 통과하면 AI 값을 쓰고 출처를 남긴다"` 가 깨졌다. 그 테스트가
`reset` 을 주입하지 않아 비교가 안 되고 채택이 0 이 된 것이다.

**그 동작 자체는 여전히 사양이다.** 달라진 것은 "같은 초기 상태에서 비교했을 때만" 이라는
전제가 붙은 것뿐이다. `reset` 을 주입해 비교 가능한 상태를 만들고 기존 기대를 그대로 둔다.

그리고 **비교 불가일 때 채택이 0 이 되는 것을 보는 테스트를 그 옆에 새로 넣는다.** 두 케이스가
나란히 있으면 무엇이 전제인지가 눈에 보인다.

```
it("baseline 이 실패하고 제안이 통과하면 AI 값을 쓰고 출처를 남긴다")
  - reset 주입. 기존 기대 그대로. "채택 1"
it("초기화 수단이 없으면 같은 상황에서도 채택하지 않는다")
  - reset 미주입. "채택 0" 과 비교 불가 사유 줄
```

**파일명은 2026-09-13 에 실재를 확인했다.** 초기화 테스트는 `reset-hook.test.ts` 가 아니라
**`reset-hook-e2e.test.ts`** 다. 계획 작성 시점에 확인하지 않고 적은 것이 틀렸다.

그 파일은 **실제 프로세스를 띄운다.** `process.execPath` 와 `-e` 로 인라인 스크립트를 돌린다.
파일 머리 주석이 규칙을 적어 두었으니 그대로 따라라.

> 임시 스크립트 파일을 만들지 않는다. `process.execPath` 와 `-e` 만 쓴다. 터미널을 병렬로
> 돌리므로 파일을 만들면 같은 경로를 두 터미널이 잡는다.

**`attemptReset` 테스트도 그 규칙을 따른다.** 임시 파일을 만들지 마라. 그리고 `CLAUDE.local.md`
가 "유닛테스트는 인메모리 + `fixtures/` 만 사용한다. 실제 서버 프로세스를 띄우는 E2E 는 직렬
전용 웨이브로 분리한다" 고 정한다. 이 파일이 이미 그 예외 자리이므로 **새 프로세스를 띄우는
테스트는 여기에만 둔다.** `pre-fill-wiring.test.ts` 와 `generate-command.test.ts` 의
상태성 fixture 는 인메모리로 만든다.

### 사양

**1) 초기화 등급.** `reset-hook.ts` 에 둔다.

```ts
/**
 * 초기화가 실제로 무엇을 했는가. `--reset-cmd` 가 0 으로 끝난 것을 "초기 상태" 라고
 * 부르지 않기 위해 있다. 명령이 무엇을 복원했는지 우리는 모른다(이슈 #399).
 */
export type ResetGrade =
  /**
   * 새 프로세스·새 연결 + 초기화 명령 성공. 우리가 제공할 수 있는 가장 강한 것.
   *
   * **이번 회차는 이 값을 내지 않는다.** 프로세스를 다시 띄우는 것은 core 의 수명주기를
   * 건드리고, generate 흐름 한가운데서 연결을 바꾸면 그 뒤 단계가 전부 새 client 를 봐야
   * 한다. 값을 미리 정의하는 이유는, commandOnly 를 "초기 상태" 라고 부르지 않으려면
   * 그보다 강한 것이 있다는 사실이 타입에 있어야 하기 때문이다. 죽은 값이 아니라
   * 의도된 자리다. 지우지 마라(설계 §3).
   */
  | "freshProcess"
  /** 같은 연결에 초기화 명령만 성공. 서버 프로세스의 메모리 상태는 그대로다. */
  | "commandOnly"
  /** 초기화 수단이 없다. 앞 실행이 바꾼 상태가 남아 있다. */
  | "none"
  /** 초기화를 시도했으나 실패했다. 상태를 모른다. */
  | "failed";

/** 등급이 "같은 초기 상태" 를 보장하는가. 후보 비교의 전제다. */
export const resetIsComparable = (grade: ResetGrade): boolean =>
  grade === "freshProcess" || grade === "commandOnly";
```

화면 문안을 표로 고정한다. **상수로 둔다. 호출부에서 문자열을 조립하지 마라.**

| 등급 | 문장 |
|---|---|
| `freshProcess` | `초기 상태: 서버를 다시 띄우고 초기화 명령을 실행했습니다.` |
| `commandOnly` | `초기 상태: 초기화 명령만 실행했습니다. 서버 프로세스의 메모리 상태는 그대로입니다.` |
| `none` | `초기 상태: 초기화 수단이 없습니다. 앞 실행이 바꾼 상태가 남아 있을 수 있습니다. --reset-cmd 로 지정하세요.` |
| `failed` | `초기 상태: 초기화 명령이 실패했습니다. 이 실행의 결과를 검증 완료로 보지 않습니다.` |

**2) 등급을 내는 함수.** 기존 `runResetCommand` 는 **그대로 둔다.** 감싸는 것을 새로 만든다.

```ts
/**
 * 초기화를 시도하고 등급을 돌려준다. 던지지 않는다. 호출부가 등급을 보고 판단한다.
 *
 * `runResetCommand` 를 그대로 두는 이유: 그 함수는 "명령이 실패하면 던진다" 는 계약이고
 * 시험 실행 직전 경로가 그 계약에 의존한다(실패하면 실행을 시작하지 않는다). 등급이
 * 필요한 자리는 판단이 다르다 — 실패해도 진행하되 무엇을 못 했는지 적는다.
 */
export async function attemptReset(
  resetCmd: string | undefined,
): Promise<{ readonly grade: ResetGrade; readonly error?: ResetCommandError }>
```

`resetCmd` 가 `undefined` 면 `"none"`, 성공하면 `"commandOnly"`, `ResetCommandError` 면
`"failed"` 다. 그 밖의 오류는 **던진다.** 우리 결함을 등급으로 위장하지 않는다.

**3) 후보 비교를 같은 초기 상태에서 한다.** `pre-fill-wiring.ts` 의 `applyPreFill` 이다.

지금은 이렇다.

```ts
const baselineRun = await run({ client, suite: subsetSuite(baseline, targetIds) });
const aiRun = await run({ client, suite: subsetSuite(baseline, targetIds, aiInputs) });
```

초기화를 주입점으로 받는다. `dryRun` 을 주입으로 받는 것과 같은 방식이다.

```ts
export async function applyPreFill(options: {
  readonly client: McpClient;
  readonly baseline: TestSuiteSpec;
  readonly preFill: PreFillResult;
  readonly dryRun?: (o: {...}) => Promise<DryRunResult>;
  /**
   * 후보 실행 앞과 사이에 부른다. 없으면 비교하지 않고 전부 baseline 을 유지한다.
   * 테스트가 실제 명령을 실행하지 않게 주입으로 받는다.
   */
  readonly reset?: () => Promise<ResetGrade>;
}): Promise<ApplyPreFillResult>
```

실행 순서:

```
1. reset()  → grade1
2. baselineRun
3. reset()  → grade2
4. aiRun
5. resetIsComparable(grade1) && resetIsComparable(grade2) 일 때만 채택 판정
```

**둘 중 하나라도 비교 불가면 채택을 하지 않는다.** 전부 baseline 을 유지하고 사유를 낸다.
이것이 이 태스크에서 가장 중요한 판단이다. 비교 조건이 안 갖춰졌으면 고르지 않는 것이 옳다.
상태 차이를 입력 개선으로 오인해 채택한 값은 재현되지 않는 명세를 만든다.

`reset` 이 `undefined` 면(주입 안 함) `"none"` 과 같이 다룬다. 비교하지 않는다.

`ApplyPreFillResult` 에 필드를 더한다.

```ts
  /** 비교를 실제로 했는가. false 면 adopted 가 0 이고 skippedReason 이 있다. */
  readonly compared: boolean;
  /** 비교를 못 한 사유. compared 가 true 면 undefined 다. 화면에 그대로 찍는다. */
  readonly skippedReason?: string;
```

`skippedReason` 문안:

```
사전보완 비교를 건너뜁니다: 두 후보를 같은 초기 상태에서 실행할 수 없습니다.
```

그 아래에 §1 표의 등급 문장을 이어 찍는다. 등급이 둘(앞·사이)이면 **더 약한 쪽**을 찍는다.
`failed` 가 `none` 보다 약하다고 보지 않는다. 둘은 다음에 할 일이 다르므로, 둘 다 있으면
**둘 다 찍는다.**

**4) `generate-command.ts` 배선.** `applyPreFill` 호출부에 `reset` 을 넘긴다.

```ts
reset: () => attemptReset(input.resetCmd).then((r) => r.grade),
```

**`--no-dry-run` 경계는 이미 두 겹으로 막혀 있다. 2026-09-13 에 코드로 확인했다.**

| 겹 | 자리 | 내용 |
|---|---|---|
| 1 | `generate-command.ts:2220` | `if (!input.dryRun) { ...안내... return skip; }` — `applyPreFill` 이 아예 안 불린다 |
| 2 | `generate-command.ts:536` | 파서가 `--no-dry-run` 과 `--reset-cmd` 동시 사용을 `UsageError` 로 거절한다 |

**새 게이트를 만들지 마라.** 2번 때문에 `resetCmd` 가 있으면 `dryRun` 은 항상 참이다.
`attemptReset` 이 `--no-dry-run` 경로에서 불릴 길이 문법적으로 없다.

이 사실이 실제로 그런지 **테스트로 고정해라.** 지금 맞다는 것과 앞으로도 맞다는 것은 다르다.

1466~1475행의 기존 시험 실행 직전 초기화는 **그대로 둔다.** 그 자리는 실패 시 실행을
시작하지 않는 계약이고 지금 동작이 옳다.

### 테스트

상태성 fixture 를 인메모리로 만든다. 실제 프로세스를 띄우지 않는다.

```
describe("attemptReset")
  it("resetCmd 가 없으면 none 이다")
  it("명령이 성공하면 commandOnly 다")
  it("ResetCommandError 면 failed 다. 던지지 않는다")
  it("그 밖의 오류는 던진다")
    - 우리 결함을 등급으로 위장하지 않는다

describe("후보 비교가 같은 초기 상태에서 돈다")
  fixture A: 도구가 첫 호출에서 실패하고 두 번째 호출부터 성공한다(호출 수를 세는 목).
             reset() 이 불리면 카운터가 0 으로 돌아간다.

  it("초기화 없이 돌리면 상태 차이로 AI 가 통과하지만 채택하지 않는다 (회귀 재현)")
    - reset 을 주입하지 않으면 baseline 실패·AI 통과가 되지만 adopted 는 0 이다
    - **이 테스트는 고친 뒤에도 남긴다.** 초기화가 왜 필요한지가 여기 적혀 있다.
      재현하려는 **기전**(입력이 아니라 상태 때문에 AI 가 통과한다)은 그대로 두고,
      그것을 근거로 채택하지 않는다는 것을 단언한다

    처음에는 `adopted 1` 로 적었다. **틀렸다.** §3 의 사양("reset 이 없으면 비교하지 않는다")과
    함께 참일 수 없다. 회귀 재현 테스트를 기존 동작 그대로 옮겨 적고 그 뒤에 사양을 바꿔
    놓고도 이 줄을 안 고친 것이다(2026-09-13 구현자가 찾음).
  it("초기화를 넣으면 둘 다 실패해 held 가 된다")
    - reset 주입 시 adopted 0, held 1, baseline 유지
  it("reset 을 후보 실행 앞과 사이에 각각 부른다")
    - 호출 횟수가 정확히 2
  it("앞 초기화가 none 이면 비교하지 않는다")
    - compared false, adopted 0, skippedReason 에 "같은 초기 상태에서 실행할 수 없습니다"
  it("사이 초기화가 failed 면 비교하지 않는다")
  it("등급이 둘 다 나쁘면 두 문장을 다 찍는다")
  it("commandOnly 두 번이면 비교한다")
    - compared true
  it("aborted 면 종전대로 전부 baseline 을 유지한다")
    - 기존 동작 회귀

describe("generate-command 배선")
  it("--no-dry-run 과 --reset-cmd 를 함께 주면 UsageError 다")
    - 기존 동작. 이 회차가 그것을 깨지 않았음을 고정한다
  it("--no-dry-run 이면 applyPreFill 이 안 불린다")
    - 주입한 applyPreFill 호출 수 0. 기존 2220행 게이트를 고정한다
  it("--no-dry-run 이면 초기화 명령이 한 번도 실행되지 않는다")
    - 호출 수 0. #397 경계
```

### changeset

`@mcpeak/cli` **minor**. 채택 동작이 바뀐다.
요약 한 줄: `사전보완 후보를 같은 초기 상태에서 비교하고, 비교할 수 없으면 채택하지 않는다`.

### 검증

```sh
pnpm build --force
pnpm --filter @mcpeak/cli test
pnpm --filter @mcpeak/cli typecheck
pnpm lint
pnpm test
```

---

## T2. 최종 명세를 처음부터 재검증한다 (`cli`)

선행: T1 이 통합 대장에 SHA 로 기록돼 있어야 한다.

### Files

수정:
- `packages/cli/src/generate-command.ts`
- `packages/cli/tests/generate-command.test.ts`
- `packages/cli/tests/generate-integration-e2e.test.ts`
- `packages/cli/src/spec-approval.ts` (**고칠 것이 있을 때만.** §1 을 봐라)
- `packages/cli/tests/spec-approval.test.ts` (위와 같다)

**확인 질문이 하나 늘면 `confirm` 호출 수를 세는 테스트가 깨진다.** 2026-09-13 실측에서
`generate-integration-e2e.test.ts` 가 `4 times → 5 times` 로 걸렸다.

**숫자만 바꾸고 끝내지 마라.** 그 숫자가 무엇을 세는지 코드에 안 남으면 다음에 질문이 또 늘
때 같은 일을 반복한다. 숫자를 바꾸되 **무엇을 세는지 주석을 남기거나**, 가능하면 호출 횟수
대신 **어느 질문이 불렸는지**를 단언한다. 후자는 순서가 바뀌어도 의미가 남는다.

같은 이유로 `gateDeps` 의 `confirms` 배열에도 순서를 적는다.

```ts
// confirms 는 순서대로 답한다. [시험 실행 계속, 저장, 최종 전량 재검증] 이다(#399).
```

그리고 **재검증만 거절하는 테스트**(`confirms: [true, true, false]`)를 넣어 세 번째 자리가
재검증이라는 것을 단언으로 고정한다. 계획서 테스트 목록의 `"거절해도 저장은 된다"` 가 그
역할을 겸할 수 있다.

생성:
- `.changeset/<임의이름>.md`

### 사양

**1) 검증 범위는 화면에만 적는다. 명세 파일에 넣지 마라.**

```ts
/** 이 케이스의 통과가 어떤 실행에서 나왔는가. 화면 문장을 만드는 데만 쓴다(이슈 #399). */
export type CaseVerification = "fullSuite" | "isolated";
```

**저장 명세에 키를 더하지 마라.** 2026-09-13 실측이다.
`packages/runner/src/spec/validation.ts:578` 이 `unknowns(entry, ["id", "status"], path, issues)`
로 `approval.cases[]` 의 모르는 키를 거절하고, 623행이 `approval` 자신도
`["fingerprint", "cases"]` 로 막는다. 키를 더하면 `validateMcpSuite` 가 명세를 거절한다.
이슈 #401 이 `GeneratedCase.pinnedFields` 로 정확히 같은 자리를 밟았다.

싣자면 `runner` 의 명세 형식을 넓혀야 하는데 **이 이슈의 범위가 아니다.** 저장 형식은 다섯
명이 함께 보는 계약이고 표시용 사실 하나 때문에 넓힐 것이 아니다. 이슈 #399 의 체크 항목도
"`approval.cases`나 화면에" 로 적혀 있어 둘 중 하나면 된다.

`stale` 도 뺐다. 명세가 바뀌었는지는 `approval.fingerprint` 가 이미 판정하고 화면이 따로
말한다. 같은 사실을 두 곳에서 세지 않는다.

`spec-approval.ts` 를 고칠 일이 없을 수도 있다. **그 파일을 열어 보고 고칠 것이 없으면
고치지 마라.** 이 타입은 `generate-command.ts` 안에만 있어도 된다.

**2) 최종 재검증 단계.** 입력값 교정이 끝난 뒤, 저장 직전이다.

```
1. attemptReset  → grade
2. 확인 화면을 찍고 묻는다
3. 승인하면 최종 suite 전량을 runDryRun
4. 그 결과가 저장 판정이다. 합집합을 쓰지 않는다
```

확인 화면:

```
최종 명세를 처음부터 다시 실행해 검증합니다.
  {등급 문장}
  실행할 케이스: {N}건 (실제 서버 호출입니다)
```

**질문은 `runDryRun` 을 부르기 **전에** 한다.** 부른 뒤에 묻는 자리가 하나라도 있으면
#397 의 경계가 무너진다.

| 상황 | 동작 |
|---|---|
| 대화형이고 승인 | 재검증한다. 결과가 판정이다 |
| 대화형이고 거절 | 재검증 안 한다. **저장은 막지 않는다.** §3 문장을 찍는다 |
| `--yes` | 묻지 않고 재검증한다 |
| 비대화형(`--yes` 아님) | 묻지 않고 **건너뛴다.** §3 문장을 찍는다 |

비대화형에서 건너뛰는 이유: 물어볼 수 없는 곳에서 실제 호출을 조용히 늘리지 않는다.
`--yes` 는 "질문에 전부 예" 이므로 그때는 승인으로 본다.

**3) 재검증을 못 했을 때의 문장.** 상수로 둔다.

```
최종 전량 재검증을 하지 않았습니다. 개별 케이스 통과 기록만 있습니다.
저장 후 `mcpeak test` 로 전체를 한 번 실행해 확인하세요.
```

등급이 `none` 또는 `failed` 라서 재검증의 의미가 약한 경우에도 **재검증 자체는 한다.**
채택(T1)과 다르다. 채택은 명세의 내용을 바꾸는 것이고 최종 검증은 이미 정해진 명세를
확인하는 것이다. 확인을 못 했다고 내용을 버릴 이유는 없다. 다만 등급 문장을 함께 찍어
그 확인이 얼마나 강한지 말한다.

**4) 케이스 사이에는 초기화하지 않는다.** 스위트 경계에서만 한다. 스위트 안의 케이스 순서는
의미가 있다(앞 케이스가 만든 것을 뒤 케이스가 읽는 명세를 사람이 쓸 수 있다). 매 케이스마다
초기화하면 우리가 검증한 것이 사용자가 나중에 실행할 것과 달라진다.

**5) 저장 화면.** 이것이 검증 범위를 남기는 **유일한 자리**다.

```
  검증 범위: 12건을 최종 전량 실행에서 확인했습니다.
```
```
  검증 범위: 3건은 개별 실행에서만 확인했습니다. 전량 재검증을 하지 않았습니다.
  저장 후 `mcpeak test` 로 전체를 한 번 실행해 확인하세요.
```

### 테스트

```
describe("최종 전량 재검증")
  fixture B: 케이스 2개. 단독으로는 둘 다 통과하고, 1번 다음에 2번을 돌리면 2번이 실패한다.

  it("합집합만 쓰면 못 잡는 실패를 잡는다")
    - 재검증을 켜면 저장 판정이 실패다
    - **이 테스트가 이 태스크의 존재 이유다**
  it("재검증 전에 초기화를 부른다")
  it("케이스 사이에는 초기화를 안 부른다")
    - 호출 횟수로 센다. 스위트 경계에서만 1회
  it("질문은 runDryRun 전에 한다")
    - 거절하면 runDryRun 호출 수가 0
  it("거절해도 저장은 된다")
    - 저장되고 §3 문장이 찍힌다
  it("--yes 면 묻지 않고 재검증한다")
  it("비대화형이면 묻지 않고 건너뛰고 안내를 찍는다")
    - runDryRun 호출 수가 0
  it("--no-dry-run 이면 재검증도 초기화도 없다")
    - 두 호출 수가 모두 0
    - 참고: 파서가 --no-dry-run 과 --reset-cmd 동시 사용을 이미 거절하므로(536행) 이 조합은
      resetCmd 없이만 만들어진다. 그래도 재검증 호출이 0 인지는 따로 봐야 한다

describe("검증 범위 화면")
  it("전량 재검증을 했으면 그렇게 찍는다")
  it("안 했으면 개별 확인 건수와 안내를 찍는다")
  it("저장된 명세 파일에 새 키가 하나도 안 생긴다")
    - 저장 결과를 validateMcpSuite 에 넣어 valid 가 true
    - 직렬화 문자열에 "verification" 이 없다
    - **이 테스트가 §1 의 존재 이유다.** 빠지면 명세가 거절당하는 결함이 조용히 들어온다
```

### changeset

`@mcpeak/cli` **minor**. 저장 형식에 키가 늘고 실행 단계가 는다.
요약 한 줄: `최종 명세를 초기 상태에서 전량 재실행해 검증하고 범위를 기록한다`.

### 검증

```sh
pnpm build --force
pnpm --filter @mcpeak/cli test
pnpm --filter @mcpeak/cli typecheck
pnpm lint
pnpm test
```

통과 기준: 루트 `pnpm test` 전건 녹색.

---

## 4. 통합 게이트 (오케스트레이터)

```sh
pnpm build --force      # Cached: 0 cached 확인
pnpm typecheck --force
pnpm lint
pnpm test
```

## 5. 완료 조건

- T1 · T2 가 통합 대장에 SHA 로 기록됐다
- 프로젝트 루트에서 네 명령이 전부 녹색이고 검사 대상 파일 수가 0 이 아니다
- `.changeset/` 에 이 브랜치들이 추가한 파일이 2개 있다
- 설계 문서 §6 의 완료 조건 1~8 이 판정된다. **1번(fixture A)과 2번(fixture B)이 이 이슈의
  존재 이유이므로 반드시 테스트로 남긴다**
