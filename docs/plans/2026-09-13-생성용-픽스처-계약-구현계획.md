# 생성용 픽스처 계약 구현계획

설계 문서: [생성용 픽스처 계약](../2026-09-13-생성용-픽스처-계약-설계.md)
대상 이슈: #390
선행: #401 이 머지돼 있어야 한다. `synthesize.ts` 와 `render.ts` 를 같이 고친다.

## 1. 실행 모델

메인 세션은 오케스트레이터다. 스폰·리뷰·머지 게이트만 하고 코드를 쓰지 않는다.

## 2. 터미널 분할과 웨이브

세 태스크가 순차다.

| 웨이브 | 태스크 | 패키지 | worktree / 브랜치 | 모델 |
|---|---|---|---|---|
| 1 | T1 값의 출처를 타입으로 만들고 픽스처를 해석 순서에 넣는다 | `generate` | `.claude/worktrees/mcpeak-390-t1` / `feat/value-provenance` | 상위 |
| 2 | T2 픽스처 파일을 읽고 실패 진단이 출처를 말한다 | `cli` | `.claude/worktrees/mcpeak-390-t2` / `feat/fixture-file` | 상위 |
| 3 | T3 교정한 값을 픽스처로 되돌린다 | `cli` | `.claude/worktrees/mcpeak-390-t3` / `feat/fixture-writeback` | 상위 |

**셋 다 상위 모델이다.** T1 은 해석 순서와 출처 판정(설계의 뼈대), T2 는 "서버 결함이
아닐 수 있다" 는 실패 문안 설계, T3 은 사용자 파일을 쓰는 경계 판단이 들어간다.

## 3. 수정 금지 공유 계약

- `packages/core/src/types.ts` 의 `McpClient` · `ToolResult`
- `packages/runner` 전체. 이번 작업은 `runner` 를 한 줄도 안 고친다
- 루트 `package.json` · `pnpm-workspace.yaml` · `turbo.json` · `tsconfig.base.json`
- `@modelcontextprotocol/sdk` 버전(1.x 고정)
- 목록 밖 의존성 추가 금지. **YAML·TOML 파서를 들여오지 마라.** 픽스처는 JSON 이다
- 커밋 · 머지 · 푸시 금지

**패키지 이름 주의.** CLI 의 실제 이름은 `@mcpeak/cli` 다. `pnpm --filter mcpeak` 는
`No projects matched the filters` 를 찍고 **종료 코드 0 으로 끝난다.**

---

## T1. 값의 출처를 타입으로 만든다 (`generate`)

### Files

생성:
- `packages/generate/src/fixtures.ts`
- `packages/generate/tests/fixtures.test.ts`
- `.changeset/<임의이름>.md`

수정:
- `packages/generate/src/schema.ts` (**필수.** `GenerateTestsErrorCode` 가 닫힌 유니온이다)
- `packages/generate/src/synthesize.ts`
- `packages/generate/src/render.ts`
- `packages/generate/src/baseline.ts`
- `packages/generate/src/index.ts`
- `packages/generate/src/violation-cases.ts` (`pinnedFields` 를 채우는 것만. §6 을 봐라)
- `packages/generate/tests/synthesize.test.ts`
- `packages/generate/tests/pre-fill.test.ts`

### 사양

**1) 출처 타입.** 설계의 뼈대다. `fixtures.ts` 에 둔다.

```ts
/** 이 값이 어디서 왔는가. 실패 진단이 "서버 결함인가 데이터 문제인가" 를 가르는 근거다. */
export type ValueProvenance =
  /** 사용자가 픽스처 파일에 적었다. 실재하는 자원을 가리킨다고 사용자가 보증한 값. */
  | "userFixture"
  /** 서버 선언의 const · default · examples · enum 에서 왔다. 서버가 준 값이다. */
  | "schemaDeclared"
  /** 우리가 type·format·pattern 만 보고 지어냈다. 어떤 자원도 가리키지 않는다. */
  | "schemaHint"
  /** AI 가 제안했고 사람이 확인했다. */
  | "aiProposed"
  /** 사람이 교정 화면에서 직접 입력했다. */
  | "humanRepaired";

/** 픽스처 파일 형식. JSON 만 받는다. */
export interface FixtureFile {
  readonly schemaVersion: 1;
  /** tools[도구][최상위 필드] = 값. */
  readonly tools?: Readonly<Record<string, Readonly<Record<string, JsonValue>>>>;
  /** notes[도구][필드] = 사람이 읽을 메모. 값에 영향을 주지 않는다. */
  readonly notes?: Readonly<Record<string, Readonly<Record<string, string>>>>;
}
```

`aiProposed` 와 `humanRepaired` 는 T1 에서 **정의만 하고 안 낸다.** T2·T3 이 쓴다.
죽은 값이 아니라 다음 태스크의 자리다. 주석에 그렇게 적어라.

**2) 파일 해석.** 던지지 않고 결과로 낸다. 화면이 사유를 찍어야 한다.

```ts
export type FixtureRead =
  | { readonly status: "ok"; readonly file: FixtureFile }
  | { readonly status: "invalid"; readonly reason: string };

/** 파일 내용을 읽어 검증한다. `reason` 은 화면에 그대로 찍는 한 문장이다. */
export function readFixtureFile(text: string): FixtureRead
```

`reason` 문안을 표로 고정한다.

| 상황 | 문장 |
|---|---|
| JSON 아님 | `픽스처 파일이 JSON 이 아닙니다. {파서 메시지}` |
| 최상위가 객체 아님 | `픽스처 최상위가 JSON 객체가 아닙니다.` |
| `schemaVersion` 이 1 아님 | `픽스처 schemaVersion 이 1 이 아닙니다: {값}` |
| `tools` 가 객체 아님 | `픽스처의 'tools' 가 객체가 아닙니다.` |
| 도구 항목이 객체 아님 | `픽스처의 'tools.{도구}' 가 객체가 아닙니다.` |
| `notes` 값이 문자열 아님 | `픽스처의 'notes.{도구}.{필드}' 가 문자열이 아닙니다.` |

**조용히 무시하지 마라.** 형식이 깨진 파일을 무시하면 사용자는 값이 왜 안 들어가는지 모른다.

**3) 해석 순서를 `synthesizeValue` 에 넣는다.** 여기가 T1 에서 가장 조심할 곳이다.

기존 순서는 `const → default → examples[0] → enum[0] → 타입별 합성` 이다. 그 **앞에**
픽스처를 넣는다.

```
1. userFixture   ← 새로 앞에 붙는다
2. const / default / examples[0] / enum[0]   → schemaDeclared
3. 타입별 합성                                 → schemaHint
```

1번이 2번보다 앞인 이유를 주석에 적어라. `default` 가 자리값인 서버가 많고, 사용자가 실재
값을 줬는데 서버의 `default` 가 이기면 픽스처 파일이 무의미해진다.

**픽스처는 최상위 필드에만 적용한다.** `synthesizeValue` 는 재귀하므로, 픽스처를 아는 것은
**루트 객체를 만드는 자리 하나**여야 한다. 재귀 안으로 픽스처를 들고 내려가면 중첩 경로
문법(#388)과 뒤엉킨다. 이번 범위는 최상위다.

구현 형태는 둘 중 하나다. **읽고 판단해서 고르고, 고른 이유를 보고에 적어라.**

| 길 | 판단 재료 |
|---|---|
| `synthesizeValue` 에 선택 인자를 더한다 | 호출부가 여럿이라 전부 확인해야 한다 |
| `render.ts` 가 합성 뒤 최상위 필드를 덮어쓴다 | 합성 규칙과 픽스처가 한 자리에서 안 만난다. 검증은 따로 해야 한다 |

어느 쪽이든 **픽스처 값이 스키마를 어기면 거절해야 한다.** 새 오류 코드를 쓴다.

```
오류 [INVALID_FIXTURE_VALUE]: 픽스처 값이 도구 선언을 만족하지 않습니다:
  {도구}.{필드}
  값: {JSON}
  선언: {스키마 JSON}
해결: 픽스처 파일의 해당 값을 선언에 맞게 고치세요.
```

`UNSUPPORTED_SCHEMA` 를 쓰지 마라. 그 코드는 `baseline.ts` 가 툴 단위로 건너뛰는 신호이고
(ADR-0036), 픽스처 오타 때문에 툴이 조용히 건너뛰어지면 안 된다. 사용자가 고칠 곳도
스키마가 아니라 픽스처 파일이다.

`GenerateTestsErrorCode` 는 `packages/generate/src/schema.ts` 의 닫힌 유니온이다
(`INVALID_TOOL` · `UNSUPPORTED_SCHEMA` · `INVALID_SCHEMA_CONSTRAINT` ·
`GENERATED_SUITE_INVALID`). 거기에 `INVALID_FIXTURE_VALUE` 를 더한다. 2026-09-13 확인.

검증은 `valueMatchesSchema` 를 쓴다. 새로 쓰지 마라.

**4) 출처를 케이스와 함께 낸다.**

```ts
export interface FieldProvenance {
  readonly tool: string;
  readonly field: string;
  readonly provenance: ValueProvenance;
}
```

`buildGeneratedCases` 와 `baseline.ts` 결과에 `provenances: readonly FieldProvenance[]` 를
싣는다. `outputContractSkips` 와 **같은 길**이다. 그 코드를 보고 따라 해라.

정상 케이스의 최상위 필드마다 한 건이다. 위반 케이스는 안 센다. 그쪽은 값을 일부러
어긴 것이라 출처를 물을 대상이 아니다.

**6) 픽스처 값이 든 필드를 사전보완에서 뺀다.**

이슈 #401 이 `GeneratedCase.pinnedFields` 를 만들었다. **그것을 재사용한다. 새 통로를
만들지 마라.**

`provenance.ts` 의 `analyzeToolProvenance` 는 스키마만 보므로, 사용자가 픽스처로 `city` 를
준 뒤에도 그 필드를 `placeholder` 로 센다. 그대로 두면 사전보완이 `city` 를 AI 에게 묻고,
픽스처 값이 실패했을 때 AI 값이 채택돼 **사용자가 실재를 보증한 값이 덮인다.** 설계 §2.2 가
`userFixture` 실패를 서버 쪽 문제로 보기로 한 것과 정면으로 어긋난다.

정상 케이스의 `pinnedFields` 에 **출처가 `userFixture` 인 필드**를 넣는다. `#401` 이 만든
`preparePreFillRequest` 의 제외 규칙이 나머지를 알아서 한다.

| 케이스 | `pinnedFields` |
|---|---|
| 기준 정상 | `userFixture` 출처 필드 전부 |
| 파생 정상(#401) | `#401` 의 규칙 + `userFixture` 출처 필드 |
| 위반 케이스 | `[]` (종전대로) |

**위반 케이스는 비워 둔다.** 사전보완이 위반 케이스를 애초에 안 고른다(`isHappyPath`).

### 테스트

```
describe("픽스처 값은 사전보완이 묻지 않는다")
  it("userFixture 출처 필드가 pinnedFields 에 들어간다")
  it("그 필드가 assistFields 에서 빠진다")
  it("픽스처가 없으면 종전대로 assistFields 에 남는다")
  it("위반 케이스의 pinnedFields 는 여전히 빈 배열이다")

describe("픽스처 파일 해석")
  it("정상 파일을 읽는다")
  it("여섯 사유가 각각 정해진 문장으로 나온다")
  it("tools 가 없어도 정상이다")
  it("빈 파일({schemaVersion:1})도 정상이다")

describe("해석 순서")
  it("픽스처가 default 를 이긴다")
    - { city: { type:"string", default:"Seoul-default" } } 에 픽스처 "Seoul" → "Seoul"
  it("픽스처가 const 를 이긴다")
    - const 는 서버가 못 박은 것이지만, 픽스처는 사용자가 실재를 보증한 것이다.
      **이 동작이 맞는지 판단이 필요하면 나에게 물어라.** 임의로 정하지 마라
  it("픽스처가 없으면 종전대로 동작한다")
    - 기존 synthesize 테스트가 전부 그대로 통과한다
  it("픽스처가 없는 필드는 영향받지 않는다")

describe("출처")
  it("픽스처 값은 userFixture 다")
  it("const·default·examples·enum 값은 schemaDeclared 다")
  it("지어낸 값은 schemaHint 다")
  it("정상 케이스의 최상위 필드마다 한 건이다")
  it("위반 케이스는 안 센다")

describe("픽스처 검증")
  it("타입이 다르면 INVALID_FIXTURE_VALUE 로 거절한다")
    - 도구·필드·값·선언이 메시지에 다 있다
  it("범위를 어기면 거절한다")
  it("enum 밖이면 거절한다")
  it("UNSUPPORTED_SCHEMA 를 쓰지 않는다")
    - 코드를 단언한다. 툴이 조용히 건너뛰어지면 안 된다
  it("선언에 없는 필드를 픽스처에 적으면 거절한다")
    - 오타를 조용히 무시하지 않는다

describe("결정론성")
  it("같은 픽스처로 두 번 생성한 결과가 canonicalJson 기준으로 같다")
  it("픽스처의 키 순서를 바꿔도 결과가 같다")
```

### changeset

`@mcpeak/generate` **minor**. 요약 한 줄:
`픽스처로 값을 지정하고 값의 출처를 기록한다`.

### 검증

```sh
pnpm build --force
pnpm --filter @mcpeak/generate test
pnpm --filter @mcpeak/generate typecheck
pnpm lint
```

---

## T2. 픽스처 파일을 읽고 실패 진단이 출처를 말한다 (`cli`)

선행: T1 이 통합 대장에 SHA 로 기록돼 있어야 한다.

### Files

수정:
- `packages/cli/src/generate-command.ts`
- `packages/cli/src/help.ts`
- `packages/cli/tests/generate-command.test.ts`

파일 읽기는 `deps.readFile` 주입점을 쓴다. `node:fs` 를 새로 import 하지 마라(§T1-0).

생성:
- `.changeset/<임의이름>.md`

### 사양

**0) 파일은 `cli` 가 읽는다. `generate` 는 텍스트만 받는다.**

2026-09-13 확인. `generate` 는 `writeFile`·`mkdir` 만 쓰고 **파일을 읽지 않는다.** `cli` 는
`GenerateCommandDependencies.readFile(path): Promise<Uint8Array>` 주입점을 갖고 있고
`generate-command.ts:1747` 이 이미 그것으로 UTF-8 을 디코드한다. 그 길을 그대로 쓴다.

```ts
new TextDecoder("utf-8", { fatal: true }).decode(await deps.readFile(path))
```

**`node:fs` 를 새로 import 하지 마라.** 주입점을 우회하면 테스트가 실제 파일 시스템을 타고,
`readEnv` 를 주입점으로 만든 ADR-0013 의 결정과도 어긋난다.

**1) `--fixtures <path>` 옵션.** 기본값은 현재 디렉터리의 `mcpeak.fixtures.json` 이다.

| 상황 | 동작 |
|---|---|
| 옵션 없고 기본 경로에 파일 없음 | 조용히 진행한다. 픽스처 없이 종전대로다 |
| 옵션 없고 기본 경로에 파일 있음 | 읽고 **읽었다는 사실을 화면에 한 줄 찍는다** |
| 옵션 있고 파일 없음 | **오류로 끝낸다.** 사용자가 명시했는데 없는 것은 오타다 |
| 파일이 깨졌음 | 오류로 끝낸다. T1 의 `reason` 을 찍는다 |

읽었다는 줄: `▸ 픽스처: {경로} (도구 {N}개, 필드 {M}개)`

**2) 실패 진단이 출처를 말한다.** 이 태스크의 핵심이다.

케이스가 실패했고 그 케이스 입력에 `schemaHint` 출처의 값이 있으면 안내를 붙인다.

```
    ℹ 이 케이스의 '{필드}' 값 {값 JSON} 은 스키마의 type 만 보고 만든 자리값입니다.
      실재하는 자원을 가리키지 않으므로 서버 결함이 아닐 수 있습니다.
      해결: {픽스처 경로} 에 {도구}.{필드} 값을 지정하세요.
```

그 필드에 `notes` 가 있으면 한 줄 더 붙인다.

```
      메모: {notes 값}
```

**필드 이름으로 추측하지 마라.** `city` · `path` · `id` 같은 이름 표를 만들지 마라.
판정 근거는 **그 값을 우리가 지어냈는가** 하나다. 그것은 추측이 아니라 사실이다.

`userFixture` · `schemaDeclared` 출처의 값에는 안내를 **안 붙인다.** 사용자가 실재를
보증했거나 서버가 준 값이므로 서버 쪽을 먼저 봐야 한다.

한 케이스에 `schemaHint` 필드가 여럿이면 **전부 나열한다.** 하나만 찍으면 사용자가 그것만
고치고 다시 실패한다.

**3) 도움말.** `--fixtures` 항목과 함께 §4 의 비밀값 경고를 넣는다.

```
픽스처 파일에 토큰·비밀번호·API 키를 적지 마세요. 이 파일의 값은 생성된 명세에 그대로
들어갑니다. 인증이 필요한 서버는 서버 기동 환경변수로 자격을 넘기고, 픽스처에는 그
자격으로 접근 가능한 자원 식별자만 적으세요.
```

`.gitignore` 에 넣으라고 권하지 마라. 픽스처 파일은 팀이 공유해야 재현된다. 공유할 수 없는
것을 거기 넣지 말라는 것이 규칙이다.

### 테스트

```
describe("--fixtures")
  it("기본 경로에 파일이 있으면 읽고 한 줄 찍는다")
  it("기본 경로에 없으면 조용히 진행한다")
  it("명시했는데 없으면 오류로 끝낸다")
  it("깨진 파일이면 T1 의 사유를 찍고 오류로 끝낸다")
  it("픽스처 값이 생성 입력에 들어간다")

describe("실패 진단이 출처를 말한다")
  it("schemaHint 값이 든 실패에 안내를 붙인다")
    - 세 줄이 다 나온다
  it("userFixture 값이 든 실패에는 안 붙인다")
  it("schemaDeclared 값이 든 실패에는 안 붙인다")
  it("notes 가 있으면 함께 찍는다")
  it("schemaHint 필드가 여럿이면 전부 나열한다")
  it("통과한 케이스에는 안 붙인다")
```

### changeset

`@mcpeak/cli` **minor**. 요약 한 줄:
`픽스처 파일을 읽고 자리값 때문에 실패한 케이스를 구분해 알린다`.

---

## T3. 교정한 값을 픽스처로 되돌린다 (`cli`)

선행: T2 가 통합 대장에 SHA 로 기록돼 있어야 한다.

### Files

수정:
- `packages/cli/src/input-repair.ts`
- `packages/cli/src/generate-command.ts`
- `packages/cli/tests/input-repair.test.ts`
- `packages/cli/tests/generate-command.test.ts`

생성:
- `.changeset/<임의이름>.md`

### 사양

**먼저 읽어라. 기존 파일을 갱신하는 쓰기 통로가 없다.**

`GenerateCommandDependencies` 의 쓰기 primitive 는 `openTemp` → `link` → `unlink` 다. 그리고
`link` 의 주석이 이렇게 적는다(2026-09-13 확인).

> `rename` 이 아니라 `link` 인 것이 요점이다. `rename` 은 대상이 있으면 말없이 덮어쓴다.
> `link` 는 EEXIST 로 실패하므로 원자성과 no-clobber 를 동시에 얻는다.
> **덮어쓸 수 있는 primitive 를 아예 두지 않는다.**

**이건 실수가 아니라 의도다.** 사용자 파일을 말없이 덮어쓰지 않으려고 그렇게 만들었다.

그래서 T3 은 **세 갈래 중 하나를 골라야 하고, 고르는 것은 내 몫이다.** 구현자는 이 절을
읽고 **BLOCKED 로 보고해라.** 임의로 primitive 를 늘리지 마라.

| 갈래 | 내용 | 대가 |
|---|---|---|
| A | `unlink` 후 `link` 로 교체한다 | 두 연산 사이에 파일이 없는 창이 생긴다. 덮어쓰기 금지의 취지를 우회하는 것이기도 하다 |
| B | 파일이 **없을 때만** 쓴다. 있으면 병합할 내용을 화면에 찍고 사용자가 직접 붙인다 | 반쪽이지만 기존 primitive 안에서 끝나고 사용자 파일을 우리가 안 고친다 |
| C | 쓰기를 이번 범위에서 뺀다. 교정한 값을 픽스처 JSON 조각으로 찍기만 한다 | §1.3 이 안 닫힌다. 다음에 다시 와야 한다 |

교정이 끝나면 그 값을 픽스처 파일에 쓸지 묻는다.

```
교정한 값 3건을 {픽스처 경로} 에 저장할까요?
  read_file.path      "./fs-sandbox/a.txt"       (사람이 입력)
  get_weather.city    "Seoul"                    (AI 제안, 사람이 확인)
  create_dir.path     "./fs-sandbox/new-dir"     (사람이 입력)
```

출처 표기는 `humanRepaired` → `(사람이 입력)`, `aiProposed` → `(AI 제안, 사람이 확인)` 다.

| 상황 | 동작 |
|---|---|
| 대화형이고 승인 | 쓴다 |
| 대화형이고 거절 | 안 쓴다 |
| `--yes` | **안 쓴다.** 안내만 찍는다 |
| 비대화형 | **안 쓴다.** 안내만 찍는다 |

`--yes` 에서 안 쓰는 이유를 주석에 적어라. 파일을 만드는 것은 사용자 작업 공간을 바꾸는
일이고, `--yes` 는 "질문에 전부 예" 이지 "내 파일을 고쳐도 좋다" 가 아니다.

**기존 파일을 덮어쓸 때 다른 항목을 잃지 마라.** 읽어서 병합하고 쓴다. 같은
`(도구, 필드)` 가 이미 있으면 새 값으로 바꾼다. `notes` 는 건드리지 않는다.

파일이 없으면 `{ "schemaVersion": 1, "tools": { ... } }` 를 새로 만든다.

**직렬화는 결정론적이어야 한다.** 도구 이름과 필드 이름을 코드 단위 오름차순으로 정렬하고
`JSON.stringify(x, null, 2)` 에 끝 개행 하나다. 순서가 흔들리면 diff 가 매번 커진다.

교정 중 최상위 필드가 아닌 값(중첩 경로)은 **안 쓴다.** 픽스처는 이번 회차에서 최상위만
받는다. 그 사실을 한 줄로 알린다.

### 테스트

```
describe("픽스처 되돌리기")
  it("승인하면 파일이 생긴다")
  it("거절하면 안 생긴다")
  it("--yes 면 안 쓰고 안내만 찍는다")
  it("비대화형이면 안 쓰고 안내만 찍는다")
  it("기존 파일의 다른 항목을 잃지 않는다")
  it("같은 필드는 새 값으로 바뀐다")
  it("notes 를 건드리지 않는다")
  it("도구·필드 이름이 코드 단위 오름차순으로 직렬화된다")
  it("두 번 써도 바이트가 같다")
  it("중첩 경로 값은 안 쓰고 안내를 찍는다")
  it("저장 후 다시 generate 하면 그 필드를 안 묻는다")
    - 통합 테스트. 이 이슈의 §1.3 이 닫히는 자리다
```

### changeset

`@mcpeak/cli` **minor**. 요약 한 줄: `교정한 값을 픽스처 파일에 저장한다`.

### 검증

```sh
pnpm build --force
pnpm --filter @mcpeak/cli test
pnpm --filter @mcpeak/cli typecheck
pnpm lint
pnpm test
```

통과 기준: 루트 `pnpm test` 전건 녹색.

---

## 4. 통합 게이트 (오케스트레이터)

```sh
pnpm build --force      # Cached: 0 cached 확인
pnpm typecheck --force
pnpm lint
pnpm test
```

## 5. 완료 조건

- T1 · T2 · T3 이 통합 대장에 SHA 로 기록됐다
- 프로젝트 루트에서 네 명령이 전부 녹색이고 검사 대상 파일 수가 0 이 아니다
- `.changeset/` 에 이 브랜치들이 추가한 파일이 3개 있다
- 설계 문서 §5 의 완료 조건 1~11 이 판정된다
