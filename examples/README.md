# examples

도그푸딩 대상 · 데모용 예제 MCP 서버가 들어갈 자리.

- "우리 도구로 우리를 검증한다." 여기의 예제 서버에 `mcptest` 를 적용하는 E2E 를 CI 에 포함한다
  (CONTRIBUTING §6). 이게 깨지면 사용자에게도 깨진 것이다.
- 아직 비어 있다. W2 에 실제 예제 서버 1개 + `npx mcptest` E2E 를 추가한다.
