# @mcptest/runner

공개 API 설계 · matcher · **실패 메시지 품질** · 리포터 · JUnit XML.

- **오너:** (이름)
- **의존:** `@mcptest/core`

## 상태

`createMcpTest` · `toContainTool` 스텁만 존재한다. 구현은 오너가 채운다.
matcher 의 실패 메시지는 이 프로젝트의 핵심 제품이다 (CLAUDE.md).
